<Page x:Class="ComputerStorePresentation.AllItems"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ComputerStorePresentation"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AllItems" Background="DarkGray" Height="450" Width="800">

    <Grid>
        <Grid Margin="0,47,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="29*"/>
                <ColumnDefinition Width="171*"/>
            </Grid.ColumnDefinitions>
            <DataGrid x:Name="ItemsGrid" AutoGenerateColumns="True" Grid.ColumnSpan="2" IsReadOnly="True"/>
        </Grid>
        <Button Click="StoreButton_Click" Content="Назад" HorizontalAlignment="Left" Margin="51,22,0,0" VerticalAlignment="Top" Width="89"/>

    </Grid>
</Page>
